# Classificador_De_Nivel_Heroi-2
É um codigo usando função, em que ele recebe um valor e diz qual o nivel do usuário, utilizando uma variável, onde pode colocar o numero de vitórias, e com base nesse número, diz em que rank o usuário está, e a quantidade de vitórias desse usuário
